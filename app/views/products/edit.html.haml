%p#notice= notice
%p#error= flash[:error]

.jumbotron
  %h2 Editing product
.container
  = render 'form'

  = link_to 'Show', category_product_path(category, product)
  \|
  = link_to 'Back', category_path(category)
